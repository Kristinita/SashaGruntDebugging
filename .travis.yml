os: linux

dist: jammy

git:
  depth: 1

language: node_js

node: node

cache:
  npm: false

install:
- npm install -g grunt-cli
- npm install

script:
- grunt postcss --verbose --stack
- more "KiraOutput/KiraExpected.html"
- more "KiraOutput/KiraBug.html"
