os: linux

dist: focal

git:
  depth: 1

language: node_js

node_js:
- node

install:
- npm install -g grunt-cli npm-check
- npm install
- npm-check node_modules/grunt-pagespeed -y

script:
- grunt pagespeed --verbose
