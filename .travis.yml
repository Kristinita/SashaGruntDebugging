os: linux

dist: jammy

git:
  depth: 1

language: python

python: nightly


install:
- python -m pip install --upgrade pip
- pip install djlint
- npm install -g grunt-cli
- npm install


script:
- grunt shell:djlint --debug --stack --verbose
