cache:
  directories:
  - $HOME/.npm
  # - $HOME/.cache/pip
  # - $HOME/.cache/pipenv

dist: noble

git:
  depth: 1

install:
- npm install --global grunt-cli
- npm install

language: node_js

node_js: node

os: linux

script:
- grunt shell:gh-pages
