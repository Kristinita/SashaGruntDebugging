cache:
  directories:
  # - $HOME/.cache/pip
  # - $HOME/.cache/pipenv
  - $HOME/.npm

dist: jammy

git:
  depth: 1

install:
- npm install --global grunt-cli
- npm ci --loglevel=error

language: node_js

node_js: node

os: linux

script:
- node --version
- npx eslint --version
- grunt shell:eslintfix --verbose
- grunt shell:eslintlint --verbose
