cache:
  directories:
  - $HOME/.npm
# - $HOME/.cache/pip
# - $HOME/.cache/pipenv

dist: jammy

git:
  depth: 1

install:
- npm install -g grunt-cli
- npm ci --loglevel=error

language: node_js

node_js: node

os: linux

script:
- grunt shell:commitlint
