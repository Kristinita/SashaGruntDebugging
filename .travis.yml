os: linux

dist: jammy

git:
  depth: 1

language: python

python: 3.12


install:
- npm install -g grunt-cli
- npm install

script:
- git log -1
- git log -1 --pretty=format:"%s%n%n%b"
- git log -1 --pretty=format:"%s%n%b"
- grunt shell:commitlint
