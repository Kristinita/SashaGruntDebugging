cache:
  directories:
  - $HOME/.npm
  - $HOME/.cache/pip
# - $HOME/.cache/pipenv

dist: noble

git:
  depth: 1

install:
- npm install --global grunt-cli
- npm ci --loglevel=error
- pip install httpie

language: node_js

node_js: node

os: linux

script:
- grunt shell:appveyorlint --verbose
